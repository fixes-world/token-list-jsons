{
  "name": "@fixes/token-list-jsons",
  "version": "1.0.0",
  "description": "The token list JSONs synced from https://token-list.fixes.world/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "TokenList",
    "Flow",
    "JSON"
  ],
  "license": "Apache-2.0",
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}